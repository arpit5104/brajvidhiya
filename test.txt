{/* ── AFFILIATES — clean list / table style ── */}
      <div className="py-20 px-6 md:px-16" style={{ background: "#f5f0e8" }}>
        <div className="max-w-7xl mx-auto">

          {/* Heading */}
          <div className="mb-14">
            <p className="text-[11px] font-bold tracking-[4px] uppercase text-[#b8962e] mb-3">Our Ecosystem</p>
            <h2 className="text-4xl md:text-5xl font-black text-[#1a3d2b]"
              style={{ fontFamily: "'Playfair Display', Georgia, serif" }}>
              Our Team is Affiliated With
            </h2>
            <div className="w-14 h-[3px] mt-4" style={{ background: "#b8962e" }} />
          </div>

          {/* Affiliates — stacked rows with top border */}
          <div>
            {affiliates.map((a, i) => (
              <div
                key={i}
                className="flex flex-col md:flex-row md:items-start gap-6 py-10"
                style={{
                  borderTop: "1px solid #d8d0c4",
                  transition: "background 0.5s ease",
                }}
                onMouseEnter={e => e.currentTarget.style.background = "rgba(184,150,46,0.04)"}
                onMouseLeave={e => e.currentTarget.style.background = "transparent"}
              >
                {/* Left — number + name + role */}
                <div className="md:w-64 flex-shrink-0">
                  <div className="text-6xl font-black leading-none mb-3 select-none"
                    style={{ fontFamily: "Georgia, serif", color: "#b8962e", opacity: 0.18 }}>
                    {String(i + 1).padStart(2, "0")}
                  </div>
                  <h3 className="font-black text-[#1a3d2b] text-lg leading-snug mb-2"
                    style={{ fontFamily: "'Playfair Display', Georgia, serif" }}>
                    {a.name}
                  </h3>
                  <span className="text-[10px] font-bold uppercase tracking-[2px] text-[#b8962e]">
                    {a.role}
                  </span>
                </div>

                {/* Vertical divider — desktop only */}
                <div className="hidden md:block w-px self-stretch"
                  style={{ background: "#b8962e", opacity: 0.2 }} />

                {/* Right — services as inline list */}
                <div className="flex-1 flex flex-wrap gap-x-8 gap-y-3 content-start pt-1">
                  {a.services.map((svc, j) => (
                    <div key={j} className="flex items-center gap-2.5">
                      <span className="w-1.5 h-1.5 rounded-full flex-shrink-0"
                        style={{ background: "#b8962e" }} />
                      <span className="text-sm text-[#3a5a4a]">{svc}</span>
                    </div>
                  ))}
                </div>
              </div>
            ))}

            {/* Bottom border */}
            <div style={{ borderTop: "1px solid #d8d0c4" }} />
          </div>

          {/* Footer note */}
          <p className="mt-8 text-xs text-[#8a9a8a] text-center tracking-wide">
            All inspection services are conducted under <span className="text-[#b8962e] font-bold">NABCB Accreditation Certificate No. IB 145</span> · ISO/IEC 17020:2012
          </p>
        </div>
      </div>